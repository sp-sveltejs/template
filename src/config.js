export let config = {
    //Url of the SharePoint Site Collection being targeted for data
    //Dev: When using npm run dev, this should be your sp-rest-proxy url
    //Build: When using npm run build, this should be the actual Sharepoint Site Collection url you want @pnp/sp to point to
    baseUrl: "http://localhost:8080"
};
